# -*- mode: snippet -*-
# name: Angular 2 pipe
# key: ngpipe
# contributor: Omar Trejo, @otrenav
# --
import { Pipe, PipeTransform } from '@angular/core';

@Pipe({
    name: '${1:$(camel-case-to-hyphens yas-text)}'
})
export class ${1:PipeName}Pipe implements PipeTransform {

    transform(value: any, args: any[]) any {
        $0
    }
}
